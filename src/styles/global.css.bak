:root {
  color-scheme: dark;
  font-family: 'Inter', 'Segoe UI', system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
  line-height: 1.6;
  background-color: #020617;
  color: #e2e8f0;
}

* {
  box-sizing: border-box;
}

body {
  margin: 0;
  min-height: 100vh;
  background: radial-gradient(circle at top, rgba(59, 130, 246, 0.25), transparent 55%),
    radial-gradient(circle at bottom, rgba(14, 165, 233, 0.15), transparent 60%), #020617;
}

.layout {
  display: flex;
  flex-direction: column;
  min-height: 100vh;
}

.site-header {
  position: sticky;
  top: 0;
  z-index: 10;
  backdrop-filter: blur(14px);
  background: rgba(2, 6, 23, 0.85);
  border-bottom: 1px solid rgba(148, 163, 184, 0.15);
}

.site-header__inner {
  width: min(1100px, 94vw);
  margin: 0 auto;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 1.25rem 0;
}

.site-header__brand {
  display: flex;
  align-items: center;
  gap: 0.65rem;
  font-weight: 600;
  font-size: 1.05rem;
  letter-spacing: 0.06em;
  text-transform: uppercase;
  color: #f8fafc;
}

.site-header__accent {
  width: 12px;
  height: 36px;
  border-radius: 9999px;
  background: linear-gradient(180deg, #38bdf8 0%, #c084fc 100%);
}

.site-header__nav {
  display: flex;
  gap: 1rem;
  font-size: 0.95rem;
}

.site-header__nav a {
  color: rgba(226, 232, 240, 0.85);
  padding: 0.4rem 0.7rem;
  border-radius: 0.75rem;
  transition: background 150ms ease;
}

.site-header__nav a.active,
.site-header__nav a:hover {
  background: rgba(59, 130, 246, 0.18);
  color: #f1f5f9;
}

.site-footer {
  margin-top: auto;
  border-top: 1px solid rgba(148, 163, 184, 0.14);
  background: rgba(2, 6, 23, 0.88);
}

.site-footer__inner {
  width: min(1100px, 94vw);
  margin: 0 auto;
  padding: 1.5rem 0;
  display: flex;
  flex-wrap: wrap;
  gap: 0.75rem;
  justify-content: space-between;
  color: rgba(203, 213, 225, 0.7);
}

img {
  display: block;
  max-width: 100%;
}

a {
  color: #38bdf8;
  text-decoration: none;
}

a:hover {
  text-decoration: underline;
}

main {
  width: min(1100px, 94vw);
  margin: 0 auto;
}

.hero {
  padding: clamp(3rem, 8vw, 6rem) 0;
}

.hero__content {
  display: grid;
  gap: 1.25rem;
}

.hero__eyebrow {
  text-transform: uppercase;
  letter-spacing: 0.42em;
  font-size: 0.75rem;
  color: rgba(148, 163, 184, 0.7);
}

.hero__body {
  max-width: 56ch;
  font-size: 1.05rem;
  color: rgba(226, 232, 240, 0.85);
}

.hero__actions {
  display: flex;
  gap: 1rem;
  flex-wrap: wrap;
}

.tech-cluster {
  margin: clamp(2.5rem, 6vw, 4rem) 0;
}

.tech-cluster__intro {
  color: rgba(203, 213, 225, 0.7);
  max-width: 58ch;
}

.tech-cluster__grid {
  margin-top: 1.5rem;
  display: flex;
  flex-wrap: wrap;
  gap: 0.75rem;
}

.tech-chip {
  padding: 0.45rem 0.85rem;
  border-radius: 0.85rem;
  background: rgba(15, 23, 42, 0.75);
  border: 1px solid rgba(59, 130, 246, 0.2);
  color: rgba(226, 232, 240, 0.8);
  font-size: 0.85rem;
  letter-spacing: 0.03em;
}

.project-list {
  margin: clamp(3rem, 8vw, 5rem) 0;
}

.project-list__intro {
  max-width: 60ch;
  color: rgba(203, 213, 225, 0.7);
}

.project-list__strips {
  margin-top: clamp(2rem, 6vw, 4rem);
  display: flex;
  flex-direction: column;
  gap: clamp(3rem, 7vw, 5rem);
}

.project-strip {
  display: grid;
  gap: clamp(1.5rem, 4vw, 3rem);
  grid-template-columns: minmax(0, 1.2fr) minmax(0, 1fr);
  align-items: center;
}

.project-strip--even {
  direction: rtl;
}

.project-strip--even .project-strip__body,
.project-strip--even .project-strip__media {
  direction: ltr;
}

.project-strip__media {
  background: transparent;
  border-radius: 1.5rem;
  overflow: hidden;
  min-height: 360px;
  position: relative;
  box-shadow: 0 25px 60px rgba(15, 23, 42, 0.65);
}

.project-strip__media canvas,
.project-strip__media img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
}

.project-strip__body {
  display: grid;
  gap: 0.75rem;
}

.project-strip__header {
  display: grid;
  gap: 0.35rem;
}

.project-strip__eyebrow {
  text-transform: uppercase;
  letter-spacing: 0.32em;
  font-size: 0.72rem;
  color: rgba(148, 163, 184, 0.7);
}

.project-strip__title {
  margin: 0;
  font-size: clamp(1.6rem, 3vw, 2.2rem);
}

.project-strip__summary {
  margin: 0;
  color: rgba(226, 232, 240, 0.75);
}

.project-strip__links {
  display: flex;
  gap: 0.75rem;
  flex-wrap: wrap;
  margin-top: 0.5rem;
}

.project-strip__description {
  color: rgba(226, 232, 240, 0.8);
  line-height: 1.65;
}

.project-strip__description h2,
.project-strip__description h3 {
  margin-top: 1.5rem;
}

.project-strip__description ul {
  padding-left: 1.3rem;
}

.project-strip__description li + li {
  margin-top: 0.45rem;
}

@media (max-width: 900px) {
  .project-strip {
    grid-template-columns: 1fr;
  }

  .project-strip--even {
    direction: ltr;
  }
}

.not-found {
  min-height: 50vh;
  display: grid;
  justify-items: start;
  align-content: center;
  gap: 1rem;
}

.not-found h1 {
  font-size: clamp(2rem, 4vw, 3rem);
  margin: 0;
}

.section-heading {
  font-size: clamp(2rem, 3vw, 3rem);
  margin-bottom: 0.75rem;
}

.tag-list {
  display: flex;
  flex-wrap: wrap;
  gap: 0.5rem;
}

.tag {
  padding: 0.35rem 0.75rem;
  border-radius: 9999px;
  background: rgba(148, 163, 184, 0.12);
  color: rgba(226, 232, 240, 0.9);
  font-size: 0.85rem;
  letter-spacing: 0.02em;
}

.button-link {
  display: inline-flex;
  align-items: center;
  gap: 0.35rem;
  padding: 0.65rem 1rem;
  border-radius: 0.75rem;
  background: linear-gradient(135deg, rgba(59, 130, 246, 0.25), rgba(59, 130, 246, 0.05));
  color: #e2e8f0;
  border: 1px solid rgba(59, 130, 246, 0.3);
  transition: transform 150ms ease, background 200ms ease;
}

.button-link:hover {
  transform: translateY(-2px);
  background: linear-gradient(135deg, rgba(59, 130, 246, 0.35), rgba(59, 130, 246, 0.1));
}
